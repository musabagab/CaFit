import 'package:flutter/material.dart';

class ExerciseCompleted extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('Exercise Complted!'),
      ),
    );
  }
}
